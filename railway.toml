[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[service]
# 5 minute timeout for large CSV uploads (default is 100s)
requestTimeout = 300
